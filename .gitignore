build/
.vs/
CMakeCache.txt
CMakeFiles/
*.user

# Visual Studio (WallDialogPlugin 은 vcxproj 로 짓는다)
x64/
*.vcxproj.user
*.aps
obj/
bin/

# SwiftPlugin 은 build.sh 가 소스 옆에 바로 떨군다 (CMake 처럼 build/ 를 안 판다)
*.dylib
*.so
*.dSYM/
.build-debug/
*.swiftmodule
*.swiftdoc
*.swiftsourceinfo
*.abi.json

# QtPlugin — Qt Creator/qmake 부산물. (그림자 빌드를 쓰면 build-* 폴더로 나간다)
build-*/
Makefile
.qmake.stash
*.o
moc_*.cpp
moc_*.h
ui_*.h
qrc_*.cpp
